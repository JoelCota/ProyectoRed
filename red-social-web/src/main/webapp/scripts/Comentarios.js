export class Comentario {
    contructor(id, contenido, comentario, respuestas) {
        this.id = id;
        this.contenido = contenido;
        this.comentario = comentario;
        this.respuestas = respuestas;
    }
}